/*
 * Service module for the "ui" plugin. Phase 1 is a stub — real utilities
 * (icon helpers, audit formatting, node name lookups, etc.) are ported
 * from the legacy webjars/main.js in Phase 2 and exposed as composables
 * alongside this dispatcher.
 */

const service = {
  /** Placeholder — replaced once real utilities are ported. */
  sample() {
    return 'plugin-ui: sample feature called'
  },
}

export default service
